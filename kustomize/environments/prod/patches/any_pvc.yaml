apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: placeholder
  namespace: placeholder
spec:
  storageClassName: ase-node-local
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
